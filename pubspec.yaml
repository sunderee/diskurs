name: diskurs
description: Dictionary and thesaurus for major languages in the Balkan area.

publish_to: "none"

version: 0.0.3+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  http: ^0.12.1
  provider: ^4.1.0
  rxdart: ^0.24.0
  shared_preferences: ^0.5.7
  url_launcher: ^5.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - config.dev.json
